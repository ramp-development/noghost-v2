import { resizeHero } from '$utils/resizeHero';
import { sizeBasedOnRatio } from '$utils/sizeBasedOnRatio';

export const home = () => {
  // resizeHero();
  // const heroVideo = document.querySelector('.section_showreel_bg') as HTMLElement;
  // const ratioFraction = 16 / 9;
  // sizeBasedOnRatio(heroVideo, ratioFraction);
  // window.addEventListener('resize', () => {
  //   sizeBasedOnRatio(heroVideo, ratioFraction);
  // });
};
